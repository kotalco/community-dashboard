const Typography = ({ children }) => {
  return (
    <div className="flex-shrink-0 flex items-center px-4">
      <h1 className="text-4xl font-nunito font-bold">{children}</h1>
    </div>
  );
};

export default Typography;
